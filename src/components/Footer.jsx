import React from 'react'
import { StyledFooter } from './styles/footer.styled'
const Footer = () => {
  return (
    <StyledFooter>
        <h2>A project with styled component in react</h2>
    </StyledFooter>
  )
}

export default Footer